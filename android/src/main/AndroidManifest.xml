<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <!-- Required for foreground service -->
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <!-- Required on Android 14+ (API 34) for dataSync foreground service type -->
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
  <!-- Required on Android 13+ (API 33) to show notifications -->
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

  <application>

    <service
      android:name=".StickyNotificationService"
      android:foregroundServiceType="dataSync"
      android:enabled="true"
      android:exported="false" />

    <receiver
      android:name=".StickyNotificationReceiver"
      android:enabled="true"
      android:exported="false" />

    <!--
      Transparent trampoline used for notification action buttons when
      closeOnAction or openAppOnAction is enabled.  Starting an Activity from
      the notification PendingIntent collapses the notification panel and
      bypasses Android 10+ Background Activity Launch restrictions.
    -->
    <activity
      android:name=".StickyNotificationTrampolineActivity"
      android:exported="false"
      android:theme="@android:style/Theme.NoDisplay"
      android:excludeFromRecents="true"
      android:taskAffinity="" />

  </application>

</manifest>
